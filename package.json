{
  "name": "json-server",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "json-server db.json --host 0.0.0.0 --port 3000",
    "start": "json-server db.json --host 0.0.0.0 --port ${PORT:-3000}"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.5"
  }
}